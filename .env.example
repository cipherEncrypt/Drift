# Optional. Leave unset to use /api proxy in local dev.
# DRIFT_API_URL=https://drift-api.driftplanes.workers.dev

# Worker secrets (set via wrangler secret put, never commit):
# POSTCARD_TREASURY_PRIVATE_KEY=hex private key for payout wallet
#
# Worker vars (worker/wrangler.toml or Cloudflare dashboard):
# POSTCARD_TREASURY_ADDRESS=NQ... treasury receives thrown postcards
# NIMIQ_RPC_URL=https://rpc.nimiqwatch.com
# NIMIQ_NETWORK_ID=24
